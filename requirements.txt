# Claude Search Library Requirements
# Install with: pip install -r requirements.txt

# Core
anthropic>=0.7.0
python-dotenv>=1.0.0

# Encryption & Security
cryptography>=41.0.0
pyotp>=2.9.0
argon2-cffi>=23.1.0
qrcode>=7.4.0

# APIs & HTTP
flask>=3.0.0
flask-cors>=4.0.0
requests>=2.31.0
gitpython>=3.1.0

# Data Processing
pyyaml>=6.0

# ChromaDB & Embeddings
chromadb>=0.4.0

# File Watching
watchdog>=3.0.0

# Scheduling
apscheduler>=3.10.0

# CLI
click>=8.4.2

# Claude desktop app collector (src/collector.py:collect_from_claude_desktop) -
# reads the Electron app's local Chromium IndexedDB/LevelDB cache. Not on
# PyPI under any of its documented names ("ccl_chrome_indexeddb",
# "ccl-chromium-reader" do not exist there) - install straight from source.
# Pulls in ccl_simplesnappy automatically.
git+https://github.com/obsidianforensics/ccl_chrome_indexeddb.git

# Development (optional)
pytest>=7.0.0
pytest-cov>=4.0.0
ruff>=0.16.0
bandit>=1.9.0
pip-audit>=2.10.0

# Android chat-capture bridge (src/android_bridge.py) - NOT a pip package.
# Requires the Android SDK's platform-tools (specifically `adb`) installed
# separately and reachable on PATH, or at the default per-platform SDK
# install location (see android_bridge.py's _resolve_adb_path()). See
# DEPLOYMENT_GUIDE.md for the full setup walkthrough (enabling Wireless
# debugging on the phone, pairing, etc).